# Copy to .env and fill real secrets (never commit .env).
# In Portainer: Stack → Environment variables / override only.
BS_ROOT_PASSWORD=CHANGEME-root
BS_DB_NAME=bookstack
BS_DB_USER=CHANGEME-user
BS_DB_PASSWORD=CHANGEME-pass
BS_APP_URL=http://books.<IP>.nip.io:8085
BS_APP_KEY=base64:CHANGEME-generate-via-bookstack
